# scttprs' scripts
A tiny set of everyday used scripts

## Encryption
- `encrypt.sh` -- AES 256 CBC encrypts `<file>` into `<file>.enc`
- `decrypt.sh` -- AES 256 CBC decrypts `<file.enc>` into `<file>`
